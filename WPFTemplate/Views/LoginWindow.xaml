<Window x:Class="WPFTemplate.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="450" Width="350"
        Icon="/Resources/icon.ico">
    
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            
            <TextBlock Text="Логин:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Пароль:" FontWeight="Bold" Margin="0,0,0,5"/>
            <PasswordBox Name="PwdBox" Height="30" Margin="0,0,0,15" Padding="5"/>

            <StackPanel Visibility="{Binding IsCaptchaVisible, Converter={StaticResource BoolToVis}}" Margin="0,0,0,15">
                <TextBlock Text="Введите символы с картинки:" Margin="0,0,0,5"/>
                
                <Border Background="LightGray" Padding="10" CornerRadius="5" Margin="0,0,0,5">
                    <TextBlock Text="{Binding CaptchaText}" FontSize="24" FontWeight="Bold" 
                               TextDecorations="Strikethrough" HorizontalAlignment="Center"/>
                </Border>
                
                <TextBox Text="{Binding UserCaptchaInput, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" 
                       HorizontalAlignment="Center" Margin="0,0,0,15" FontWeight="Bold"/>

            <Button Content="Войти" 
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=PwdBox}" 
                    IsEnabled="{Binding IsLoginEnabled}"/>
            
        </StackPanel>
    </Grid>
</Window>