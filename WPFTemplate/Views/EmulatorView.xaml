<UserControl x:Class="WPFTemplate.Views.EmulatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Интеграция с эмулятором" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="URL Эмулятора:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding ApiUrl, UpdateSourceTrigger=PropertyChanged}" Height="25" Padding="2"/>
            
            <Button Grid.Column="2" Content="Получить данные" Command="{Binding FetchDataCommand}" 
                    Width="150" Height="30" Margin="10,0,0,0" Background="#3498DB" Foreground="White" FontWeight="Bold"/>
        </Grid>

        <GroupBox Grid.Row="2" Header="Ответ сервера (JSON)">
            <TextBox Text="{Binding ResponseText}" 
                     IsReadOnly="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" 
                     FontFamily="Consolas" 
                     Background="#F8F9F9" 
                     Margin="5"/>
        </GroupBox>
    </Grid>
</UserControl>