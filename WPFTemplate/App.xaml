<Application x:Class="WPFTemplate.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPFTemplate"
             xmlns:views="clr-namespace:WPFTemplate.Views"
             xmlns:viewModels="clr-namespace:WPFTemplate.ViewModels"
             StartupUri="Views/LoginWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BgPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BgSecondary" Color="#7FFF00"/>
        <SolidColorBrush x:Key="AccentColor" Color="#00FA9A"/>
        <SolidColorBrush x:Key="HighlightColor" Color="#2E8B57"/>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{StaticResource BgPrimary}"/>
            <Setter Property="WindowStartupLocation" Value="CenterScreen"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <DataTemplate DataType="{x:Type viewModels:BaseCrudViewModel}">
            <views:UniversalCrudView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:EmulatorViewModel}">
            <views:EmulatorView/>
        </DataTemplate>
    </Application.Resources>
</Application>
